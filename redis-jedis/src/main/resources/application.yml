spring:
  application:
      name: redis-jedis
  redis:
    database: 5
    password:
    sentinel:
      master:
      nodes:

security.ignored: /**
spring.security.enabled: false
management.security.enabled: false
security.basic.enabled: false

server:
  contextPath: /redis-jedis
  port: 8001

